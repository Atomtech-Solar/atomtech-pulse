# Railway config: quando Root Directory = "backend"
# Usa Dockerfile (com pnpm via corepack) em vez de Nixpacks
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "pnpm start"
